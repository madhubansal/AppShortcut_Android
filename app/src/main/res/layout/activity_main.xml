<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.appshortcuts.MainActivity">

    <Button
        android:id="@+id/add_shortcut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add shortcut for google.com" />

    <Button
        android:id="@+id/remove_shortcut"
        android:layout_below="@+id/add_shortcut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Remove shortcut for google.com" />

    <Button
        android:id="@+id/add_activity_shortcut"
        android:layout_below="@+id/remove_shortcut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add shortcut for Activity" />
    <Button
        android:id="@+id/remove_activity_shortcut"
        android:layout_below="@+id/add_activity_shortcut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Remove shortcut for Activity" />

    <Button
        android:id="@+id/rank_shortcut"
        android:layout_below="@+id/remove_activity_shortcut"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Rank shortcuts" />

</RelativeLayout>
